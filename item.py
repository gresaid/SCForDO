from pynput.keyboard import Controller


def blink_dagger(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def soul_ring(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def ethereal_blade(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def shiva_guard(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def hex_d(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def laser(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def rocket(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def matrix(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)


def rearm(key):
    keyboard = Controller()
    keyboard.press(key)
    keyboard.release(key)
